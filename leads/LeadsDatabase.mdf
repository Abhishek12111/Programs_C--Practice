CREATE TABLE Leads (
    LeadID VARCHAR(36) NOT NULL,
    FirstName VARCHAR(50),
    LastName VARCHAR(50),
    Email VARCHAR(100),
    -- Add other lead-related fields as needed
    PRIMARY KEY (LeadID)
);
